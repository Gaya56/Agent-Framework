name: "Brave Search"
description: "Web, image, video and news search via Brave API"
container_name: "agent-framework-mcp-brave-search-1"
server_path: "/app"
icon: "🔍"
enabled: true
client_class: "clients.brave_search_client.BraveSearchClient"
tools:
  brave_web_search:
    description: "Perform a web search"
    parameters:
      query: "Search term"
      count: "Number of results"
      safesearch: "Search safety level"
      freshness: "Time filter"
  brave_image_search:
    description: "Search for images"
    parameters:
      searchTerm: "Search term"
      count: "Number of images"
      safesearch: "Search safety level"
  brave_video_search:
    description: "Search for videos"
    parameters:
      query: "Search term"
      count: "Number of results"
      freshness: "Time filter"
  brave_news_search:
    description: "Search for news articles"
    parameters:
      query: "Search term"
      count: "Number of results"
      freshness: "Time filter"
  brave_local_search:
    description: "Search for local businesses"
    parameters:
      query: "Search term"
      count: "Number of listings"
